export function RescuePlanCard() {
  return (
    <section className="rounded-lg border border-emerald-200 bg-emerald-50 p-5">
      <h2 className="text-lg font-bold text-emerald-950">Decision support only</h2>
      <p className="mt-2 text-sm text-emerald-900">
        Risk Sentinel never takes custody, never asks for private keys, and never auto-trades. Rescue estimates are approximations for planning.
      </p>
    </section>
  );
}
