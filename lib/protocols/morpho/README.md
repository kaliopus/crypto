# Morpho adapter placeholder

Risk Sentinel V0 implements Aave V3 only. Morpho support should implement the
`ProtocolAdapter` interface in `lib/protocols/types.ts` and normalize market or
vault risk into the same `PositionRisk` shape.
